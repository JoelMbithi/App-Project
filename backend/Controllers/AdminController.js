


//ApI to adding doctors

const addDoctor = async(req,res) => {
    try {

        const { name, email, password, speciality, about, fees, address } = req.body
        
    } catch (error) {
        
    }
}

export {addDoctor}